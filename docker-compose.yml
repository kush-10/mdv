services:
  mdv-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:4173"
    env_file:
      - .env
    volumes:
      - mdv_data:/data
    restart: unless-stopped

volumes:
  mdv_data:
